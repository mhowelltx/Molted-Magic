#!/usr/bin/env bash
# Placeholder — Session 4. Writes openclaw.json from the template, sets a
# narrow workspace directory, cheap default model, minimal tool allowlist,
# and wires the Telegram/BotFather channel. Must be idempotent.
set -euo pipefail
