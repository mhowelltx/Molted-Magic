#!/usr/bin/env bash
# Placeholder — Session 4. Installs Node 22+, then runs the official OpenClaw
# installer (review the fetched script before executing — do not blind-pipe
# to bash). Must be idempotent: safe to re-run.
set -euo pipefail
