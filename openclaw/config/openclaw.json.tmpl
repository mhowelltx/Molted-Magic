{
  "_comment": "Placeholder — Session 5. Templated config: agents as a list (see ROADMAP.md Extensibility notes), model, tool allowlist, workspace path. No real secrets inline — placeholders only, filled by configure.sh at deploy time.",
  "agents": []
}
