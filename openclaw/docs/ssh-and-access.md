# SSH and access

Placeholder — operational notes on reaching the box (Tailscale address, SSH key location, how to open a session) once it exists. Kept as docs, not code, since it's reference material rather than something Terraform/scripts consume.
